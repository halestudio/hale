# Lines that start with '#' are comments.
# For a project mostly in C, the following would be a good set of
# exclude patterns (uncomment them if you want to use them):
# *.[oa]
# *~

# generic part (can be overridden locally using !pattern)
bin/
dist/
logs/
classes/
target/
/_*/
!platform/target/
!/**/src/**/target/
!legacy/plugins/eu.esdihumboldt.commons/target/
*.patch
/build/build/
/build/workspace/
/workspace/
pom.xml
.metadata/
.recommenders/

# generic main project
/.project

# all hail windows
Thumbs.db

# and mac too
.DS_Store

# and linux too
*~

# backup files (ex. vi)
*.swp

#.gitignore
*.hprof
*.tycho-consumer-pom.xml
